package com.keks.sf.soap

import utils.TestBase


class SfTableDescriptionTest extends TestBase {

//  "SfRelation#getRequiredPredefinedSchema" should "filter and reorder columns" in {
//    val cols = Array(ID, SYSTEMMODSTAMP, LASTMODIFIEDDATE)
//    val schema = StructType(Array(
//      StructField(TIME_FIELD, StringType),
//      StructField(SYSTEMMODSTAMP, StringType),
//      StructField(LASTMODIFIEDDATE, StringType),
//      StructField(ID, StringType),
//      StructField(NAME, StringType)))
//
//    val res: StructType = SfRelation.getSoqlAndSchemaBasedOnRequiredCols(cols, schema)
//    val exp = StructType(Array(
//      StructField(ID, StringType),
//      StructField(SYSTEMMODSTAMP, StringType),
//      StructField(LASTMODIFIEDDATE, StringType)))
//
//    assert(exp == res)
//  }

}
